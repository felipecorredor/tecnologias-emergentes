// project import
import dashboard from './dashboard';
import students from './students';
import teachers from './teachers';
import subjects from './subjects';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
  items: [dashboard, students, teachers, subjects]
};

export default menuItems;
